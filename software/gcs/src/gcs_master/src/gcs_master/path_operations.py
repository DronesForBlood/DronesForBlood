#!/usr/bin/env python3
"""
Module containing a path decision-making function.
"""

def get_new_goal(current_pos, current_dest, home_pos, rally_point, batt_level,
                 blocking_time=10, speed=5, energy_consumption=1):
    """
    Function for deciding the new goal

    All the input coordinates are in the format [LAT, LON] (deg). 

    returns a [int, int] list with [X, Y] coordinates of new destination
    """
    new_destination = [None, None]
    return new_destination


def deg2meters():
    return
